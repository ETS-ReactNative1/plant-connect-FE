describe("Camera modal feature flow", () => {

  it('should allow user to view the camera modal', () => {
    cy.visit('http://localhost:19000/')
  })

  it('should allow user to view a camera icon', () => {
    cy.visit('http://localhost:19000/')
  })
  
})
